# Engineering Journal — Day 1: 2026-09-18

## Issue Worked On
Engineering Setup — GitHub MCP & Docker Integration

## What I Did
- Configured GitHub MCP integration for Antigravity to enable tool-based repository interactions.
- Created a fine-grained GitHub Personal Access Token (PAT) scoped strictly to the `Submitly` repository with the required repository permissions.
- Set up Docker Desktop on the local environment to run the containerized GitHub MCP server.
- Verified that the Docker Engine daemon is active and functioning properly.
- Configured the GitHub MCP server container with the required environment variables and credentials.
- Verified the MCP connection and tool execution by invoking the GitHub MCP `list_issues` tool.
- Confirmed the current state of the repository issues: two closed issues (#1 and #2) and zero open issues.

## Engineering Decisions
- **Fine-Grained Scoped Token:** Used a fine-grained PAT limited exclusively to the `Submitly` repository instead of a classic personal access token, adhering to the principle of least privilege.
- **Containerized MCP Execution:** Leveraged Docker Desktop to run the official GitHub MCP server in an isolated container environment, ensuring reproducible execution and clean dependency separation.

## Implementation
- Created the project engineering documentation structure: `Docs/Engineering Journal/`.
- No application code or database models were modified during this setup task.

## Verification
- **Docker Engine Check:** Validated Docker status to ensure containers can spin up and communicate over local sockets.
- **GitHub MCP Tool Call:** Executed `list_issues` via the GitHub MCP server, successfully fetching repository issue status.
- **Repository State Verification:** Confirmed that issues #1 and #2 are closed and no open issues remain prior to starting Issue #3.

## Key Learning
- Running MCP servers in Docker containers provides an isolated, consistent runtime environment for external integrations without polluting the host environment with server runtimes.
- Scoping credentials at the repository level ensures secure and least-privilege access for agentic tooling.
- Integrating MCP tools directly into Antigravity allows seamless issue querying and management as part of an automated development workflow.

## Next
#3 — Implement Web Authentication & Role-Based Dashboard Access
